# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release
# .gitignore
cache/tfidf_features_1000.pkl

# APK files (too large for GitHub)
*.apk
Personify.apk

# Large files that should not be in git - Now using Git LFS for large files
# Cache files (temporary, should not be tracked)
cache/*.pkl
cache/*.csv

# Large APK files should not be in git - use GitHub Releases instead
*.apk
Personify.apk
build/app/outputs/flutter-apk/*.apk

# Include model training scripts but exclude generated models
!model_training/*.py

# Environment variables and API keys - NEVER COMMIT THESE
.env
.env.local
.env.*.local
config.json
secrets.json
api_keys.json

# Firebase and other service account keys
**/google-services.json
**/GoogleService-Info.plist
firebase_options.dart

